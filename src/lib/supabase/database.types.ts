// Placeholder types so the project compiles before you've generated the
// real ones. Once your Supabase project is migrated, run:
//
//   npm run db:types
//
// (with SUPABASE_PROJECT_ID set in your shell) to overwrite this file with
// accurate generated types from your live schema.

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
